listener 1883
allow_anonymous true

# The official Mosquitto Docker image starts in a locked-down default mode that refuses all connections unless you provide a config file. 
# This two-line file is the minimum needed:
# listener 1883 — accept TCP connections on port 1883.
# allow_anonymous true — allow clients to connect without a username or password. In a real deployment you would configure proper authentication. 